export const metadata = {
    title: "Code First, Understanding Follows"
}

<CodeBlock 
language="code-first"
children={`let solution = build(minimalSolution)

while (!satisfied(solution)) {
    solution = optimize(solution)
}`}
/>

I think through code. I build early, observe behavior, and refine continuously. For complex systems, I try to prototype intentionally — isolating layers, testing assumptions, and iterating toward stability. Writing code is how I explore the edges of a problem.